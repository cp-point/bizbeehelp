import Loading from '../../components/atom/Loading';

const AppLoading = () => {
    return <Loading label="불러오는 중입니다." size="38px" color="#1f8a5b" thickness="4px" fullScreen />;
};

export default AppLoading;
